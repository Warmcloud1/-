電腦運行時 CPU、RAM、硬碟 ，最快CPU 第二快RAM 硬碟最慢 ，程式跑時，快的要配合慢的

JAVA的檔案處理，皆用"串流Stream"傳 ，不管是何種硬體及作業系統環境下，檔案都被視為bits及characters所組成的串流。檔案都會經過硬碟處理裡 即寫檔或讀檔(很慢)

RAM: 快閃記憶體 電腦關機在開機不會存在任何東西

副檔名的用途:.t 英文字母t只是一種mark，依照mark可以知道副檔名的規律是如何跑，註:.txt為開源

Buffer緩衝區:在記憶體裡。寫檔有個緩衝區跟讀檔有個緩衝區一次讀多一點寫多一點至緩衝區，可以減少記憶體與硬碟交互的次數
