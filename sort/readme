常見排序法
運作速度以時間複雜度  、空間複雜度 可量
時間複雜度:有效得程式碼速度，時間複雜度又有最差(worst case)時間與平均時間跟best  用O(N)表示，念做big-O N 註:算出來是O(N/2)X -> O(N) /假設N為極大值，2會省略  //其中Log底數是2
空間複雜度:額外需要的空間 EX呼叫遞迴需要額外記憶體空間

                                                                                          |best     |最差時間複雜度|平均時間複雜度|空間複雜度  
一基本(慢)
1.氣泡排序法:從第一筆開始，逐一比較相鄰兩筆資料，比較大小後，決定是否交換                       |O(N)     |O(N^2)       |O(N^2)       |O(1)
2.插入排序法:|在已經排好的i的序列裡插一筆未排列的新資料，便可得到i+1個資料|O(n^2)               |O(N)     |O(N^2)       |O(N^2)       |O(1)
3.選擇排序法:|在未排列好的資料中，尋找最大或最小的值放在已排好的頭或尾巴                        |O(N^2)   |O(N^2)       |O(N^2)       |O(1)
二較快的排序
4.快速排序法:|a{先有一個基準點，左邊放比基準點小的，右邊放比基準點大的}，從左右邊都再進行一次a{}  |O(NlogN)  |O(N^2)      |O(NlogN)      |O(logN)~O(N)
5.合併排序法:先拆分成很多小的陣列，合併時將其排列好，一值合併後即可以得到排列好的值              |O(NlogN)  |O(NlogN)   |O(NlogN)      |O(N)
