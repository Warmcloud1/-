物件導向的特性 封裝、繼承、多型
物件導向是為多型存在的，封裝繼承皆為多型在鋪墊

封裝(Encapsulation):將屬性及方式包裝成一個類，使用存取修飾詞將內部資料隱藏，限制外部的使用

繼承(Inheritance):子類可以有限制的使用父類別的屬性與方法，父類如果用private就無法使用，子類可以再擴充，如果想改變父類的方法，可使用Override改變方法主體。

多型(Polymorphism):父類別的宣告可以指向不同的子類別物件，在執行時能夠指向不同的子類別物件 例如: Aninmal a = new dog();
